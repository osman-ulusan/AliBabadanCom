@model IEnumerable<Entity.Entity.Ilan>
@{
    ViewBag.Title = "Home Page";
}

<section id="aa-popular-category">
    <div class="container">
        <div class="row">
            <div class="col-md-12">
                <div class="row">
                    <div class="aa-popular-category-area">
                        <!-- start prduct navigation -->
                        <!-- Tab panes -->
                        <div class="tab-content">
                            <!-- Start men popular category -->
                            <div class="tab-pane fade in active" id="popular">
                                <ul class="aa-product-catg">
                                    <!-- start single product item -->
                                    @foreach (var item in Model)
                                    {
                                    <li>
                                        <figure>
                                            <a class="aa-product-img" href="#"><img src="~/Content/images/@item.Images.Image1" alt="@item.Images.Image1" height="300" width="250"></a>
                                            <a class="aa-add-card-btn" href="/Home/UrunDetay/@item.Id"><span class="fa fa-money"></span>İncele</a>
                                            <figcaption>
                                                <h4 class="aa-product-title"><a href="#">@item.Title</a></h4>
                                                <span class="aa-product-price">Görüntülenme : </span><span class="aa-product-price"><b> @item.GörüntülenmeSayisi </b></span>
                                            </figcaption>
                                        </figure>
                                        <div class="aa-product-hvr-content">
                                            <a href="#" data-toggle="tooltip" data-placement="top" title="Add to Wishlist"><span class="fa fa-heart-o"></span></a>
                                            <a href="#" data-toggle="tooltip" data-placement="top" title="Compare"><span class="fa fa-exchange"></span></a>
                                            <a href="#" data-toggle2="tooltip" data-placement="top" title="Quick View" data-toggle="modal" data-target="#quick-view-modal"><span class="fa fa-search"></span></a>
                                        </div>
                                        <!-- product badge -->
                                        @if (item.GörüntülenmeSayisi == 0)
                                        {
                                            <span class="aa-badge aa-sale" href="#">YENİ ÜRÜN</span>
                                        }
                                        else
                                        {
                                            <span class="aa-badge aa-sale" href="#">POPÜLER</span>
                                        }

                                    </li>
                                    }
                                </ul>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>
